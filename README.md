# binance-futures-testnet-bot
Developed a Binance Futures Testnet Trading Bot in Python that allows users to place Market and Limit orders through a command-line interface (CLI). The project uses the python-binance library to interact with the Binance Futures Testnet API, validates user inputs, handles exceptions, logs API requests and responses.
